---
import { getCollection } from "astro:content";

// Layout import — provides basic page elements: <head>, <nav>, <footer> etc.
import BaseLayout from "../layouts/BaseLayout.astro";
import Card from "../components/Card.astro";
import PortfolioPreview from "../components/PortfolioPreview.astro";


const grammars = ["I. Prefissi", "II. Suffissi", "III. Utilizzo delle S"];

// Content Fetching: List four most recent work projects
const letters = (await getCollection("work"))
	.sort((a, b) => b.data.publishDate.valueOf() - a.data.publishDate.valueOf())
	.slice(0, 4);
---

<BaseLayout>
	<div class="mx-auto my-24 font-serif">
		<div class="">
			<h1 class="text-5xl sm:text-8xl mb-3">
				Dizionario enciclopedico <br /> Pis - Italiano
			</h1>
			<h3 class="">Edizione I</h3>
			<h3 class="">07/11/2024</h3>
			<div
				class="fixed bottom-10 right-10 flex items-center justify-center gap-x-6"
			>
				<a
					href="#"
					class="rounded-md bg-indigo-600 px-3.5 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-indigo-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600"
					>Una voce a caso</a
				>
			</div>

			<div
				class="mt-10 flex flex-col gap-y-10 mx-auto lg:flex-row justify-center gap-x-10 text-left"
			>
				<Card title="Dizionario">
					<ul class="text-gray-900 flex flex-col gap-y-4 basis-1/2">
						{
							letters
								.map((project) => (
									<li>
										<a href={`/work/`+ project.slug} class="hover:underline">
											{project.data.title}
										</a>
									</li>
								))
						}
					</ul>
					<ul class="text-gray-900 flex flex-col gap-y-4 basis-1/2">
						{
							letters
								.map((letter) => (
									<li>
										<a href="" class="hover:underline">
											{letter}
										</a>
									</li>
								))
								.slice(12, 24)
						}
					</ul>
				</Card>
			</div>
		</div>
	</div>
</BaseLayout>
